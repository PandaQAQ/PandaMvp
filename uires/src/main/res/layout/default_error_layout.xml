<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/state_error_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/icon_default_error" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="160dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/state_error_hint"
            android:layout_width="wrap_content"
            android:layout_height="64dp"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:lineSpacingMultiplier="1.1"
            android:maxWidth="220dp"
            android:text="账单数据加载失败，"
            android:textColor="@color/res_colorTextMinor"
            android:textSize="22sp" />

        <TextView
            android:id="@+id/state_error_refresh"
            android:layout_width="wrap_content"
            android:layout_height="64dp"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:lineSpacingMultiplier="1.1"
            android:maxWidth="220dp"
            android:text="请点击重试"
            android:textColor="#008580"
            android:textSize="22sp" />
    </LinearLayout>
</LinearLayout>